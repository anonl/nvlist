@@

require("builtin/vn")

clickCount = 0

function clickHandler()
    clickCount = clickCount + 1
end

button = Gui.createButton()
button:setClickHandler(clickHandler)

button2 = Gui.createButton()
button2:setClickHandler(clickHandler)

@@